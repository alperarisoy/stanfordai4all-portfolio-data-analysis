import pandas as pd
from pathlib import Path

# Small portfolio analysis project
# I made this to practice price data, returns and volatility.
# This version uses sample data so it is easy to share.

ROOT = Path(__file__).resolve().parents[1]
DATA_FILE = ROOT / "data" / "prices.csv"


def load_data():
    # csv dosyasını okuyorum
    prices = pd.read_csv(DATA_FILE)

    # tarihi index yapmak daha düzenli oluyor
    prices["date"] = pd.to_datetime(prices["date"])
    prices = prices.set_index("date")

    return prices


def calculate_returns(prices):
    # bir önceki güne göre yüzde değişim
    returns = prices.pct_change()

    # ilk satır NaN oluyor
    returns = returns.dropna()

    return returns


def print_summary(returns):
    print("\nAsset summary:")

    for asset in returns.columns:
        avg_return = returns[asset].mean()
        volatility = returns[asset].std()

        print(asset)
        print("  avg daily return:", round(avg_return * 100, 3), "%")
        print("  daily volatility:", round(volatility * 100, 3), "%")


def calculate_portfolio(returns):
    # Bu weightler sadece basit deneme için.
    weights = {
        "tech": 0.35,
        "bank": 0.25,
        "energy": 0.20,
        "bond": 0.20
    }

    portfolio_returns = 0

    for asset, weight in weights.items():
        portfolio_returns = portfolio_returns + returns[asset] * weight

    return portfolio_returns, weights


def main():
    prices = load_data()
    returns = calculate_returns(prices)

    print("First rows of price data:")
    print(prices.head())

    print_summary(returns)

    portfolio_returns, weights = calculate_portfolio(returns)

    total_return = (1 + portfolio_returns).prod() - 1
    average_return = portfolio_returns.mean()
    volatility = portfolio_returns.std()

    print("\nPortfolio weights:")
    print(weights)

    print("\nPortfolio result:")
    print("total return:", round(total_return * 100, 2), "%")
    print("average daily return:", round(average_return * 100, 4), "%")
    print("daily volatility:", round(volatility * 100, 4), "%")

    print("\nNote:")
    print("This is a learning project, not a professional investment model.")


main()
